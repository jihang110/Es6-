(function(){/*code*/}());
1.函数命名
  function xx(){
    /*code*/
  }
函数声明以function关键字开头，接着是必须的函数（变量）名和以逗号分隔的可选的参数列表，
再接着就是以大括号封装的函数体。函数声明必须是一个单独的JavaScript语句。
2.函数表达式
var foo = function(){ /* code */ };
foo();
在任何情况下都是其它JavaScript语句的一部分（比如赋值表达式等号的右侧、函数的参数）的函
数被称为函数表达式。

先用一个大()把其设置为函数表达式，在再后面用一个(),使其立即执行
